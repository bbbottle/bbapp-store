{
    "private": true,
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "build": "yarn workspaces run build",
        "release": "node scripts/generate_app_meta_info.js && release-it --ci"
    },
    "release-it": {
        "github": {
            "release": true
        },
        "npm": {
            "release": false
        }
    },
    "devDependencies": {
        "glob": "^7.1.6",
        "release-it": "^14.6.0",
        "semver": "^7.3.5",
        "shelljs": "^0.8.4"
    },
    "version": "0.0.10"
}
